using System;
using System.Collections.Generic;
using System.Text;

namespace Controller
{
    public class WagonController
    {
        public void showWagonInfo()
        {
            /*
            Wagon w = Wagon.find(1);
            labelCapacity = w.getCapacity();
            labelSize = w.labelSize();

             */
        }

        public void updateWagon()
        {
            /*
            Wagon w = Wagon.find(2);
            w.setCapacity(1200);
            w.setSize(800);
            w.save();
             */
        }


    }
}
